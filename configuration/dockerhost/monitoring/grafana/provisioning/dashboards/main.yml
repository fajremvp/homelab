apiVersion: 1

providers:
  - name: 'Homelab Dashboards'
    orgId: 1
    folder: 'Infraestrutura'
    type: file
    disableDeletion: false
    allowUiUpdates: true
    updateIntervalSeconds: 30 # Verifica mudanças no JSON a cada 30s
    options:
      path: /var/lib/grafana/dashboards
